{
  "name": "smart-business-ai-assistant",
  "private": true,
  "version": "1.0.0",
  "description": "Root workspace for Smart Business AI Assistant",
  "scripts": {
    "setup": "npm --prefix backend ci && (npm --prefix frontend ci || npm --prefix frontend install)",
    "dev:mongo": "docker compose up -d mongo",
    "dev:backend": "set MONGODB_URI=mongodb://localhost:27018/smart-business-ai && npm --prefix backend run dev",
    "dev:frontend": "set VITE_API_BASE=http://localhost:5000 && npm --prefix frontend run dev",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev": "npm run setup && npm run dev:mongo && npm run dev:all"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
